#include <stdio.h>
struct
{
	int total;
	int current;
} year;
int main()
{
	scanf("%d", year.total);
	year.current = 0;
	char receive[year.total];
	int medal[3][year.total], score;
	for (int i = 0; i < year.total; i++)
	{
		
	}
	for (int i = 0; i < year.total-3; i++)
	{
		
	}
	return 0;
}
