#include <stdio.h>
#include <string.h>
int main()
{
	int length, value = 0;
	scanf("%d", &length);
	char command[length];
	scanf("%s", command);
	for (int pointer = 0; pointer < length; pointer++)
	{
		switch()
	}
	return 0;
}
