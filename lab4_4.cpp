#include <stdio.h>
#include <string.h>
int main()
{
	char message[1000] = {};
	scanf("%s", message);
	for (int i = 0; i < 1000; i++)
	{
		// a an and at for i in of on the with
		switch (message[i])
		{
			
		}
	}
	return 0;
}
